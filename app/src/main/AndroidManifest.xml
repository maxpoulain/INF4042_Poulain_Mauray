<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.esiea.mauray_poulain.appmobileproject" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AppMobileProject"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="AppMobileProject"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondeActivity"
            android:label="SecondeActivity" >
        </activity>
        <activity
            android:name=".ThirdActivity"
            android:label="ThirdActivity" >
        </activity>

        <!--<service
            android:name=".GetBiersServices"
            android:exported="false" >
        </service>
        <service
            android:name=".GetDescriptionBier"
            android:exported="false" >
        </service>-->
        <service
            android:name=".GetBierServices"
            android:exported="false" >
        </service>
    </application>

</manifest>
